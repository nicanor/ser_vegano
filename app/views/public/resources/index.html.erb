<section class="blog">
  <h1><%= t :resources %></h1>
  <p class="lead"><%= t :resources_explanation %> <%= link_to t(:visit_facebooK), facebook_url, target: '_blank' %></p>
  <div class="bor"></div>

  <section id="posts pblock">
    <% @resources.each_slice(3) do |resources_for_row| %>
      <div class="row">
        <%= render partial: 'resource', collection: resources_for_row %>
      </div>
    <% end %>
  </section>

</section>