<section class="navbar bs-docs-nav" role="banner">
  <div class="container">
    <div class="navbar-header">
      <button class="navbar-toggle" type="button" data-toggle="collapse" data-target=".bs-navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div>
    <nav class="collapse navbar-collapse bs-navbar-collapse" role="navigation">
    <!-- Navigation links starts here -->
      <ul class="nav navbar-nav">

        <li><%= link_to t(:home), root_path %></li>

        <% if @static_pages.any? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= t :brand %> <b class="caret"></b></a>
            <!-- Submenus -->
            <ul class="dropdown-menu">
              <% @static_pages.each do |page| %>
                <li><%= link_to page.title, page %></li>
              <% end %>
            </ul>
          </li>
        <% end %>

        <li><%= link_to t( :articles), articles_path %></li>
        <li><%= link_to t(  :recipes), recipes_path %></li>
        <li><%= link_to t(:resources), resources_path %></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to t(:admin), admin_root_path if logged_in? %></li>
      </ul>
    </nav>
  </div>
</section>